{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "types": ["react", "react-native"],
    "jsx": "react-jsx",
    "skipLibCheck": true,
    "strict": false,
    "baseUrl": ".",
    "paths": {
      "@env": ["types/env"]
    }
  }
}
